require('dotenv').config()

module.exports = {
    APP_PORT,
    APP_HOST,
    MYSQL_HOST,
    MYSQL_PORT,
    MYSQL_DATABASE,
    MYSQL_ROOT,
    MYSQL_PASSWORD,
    AVATAR_PATH,
    BANNER_PATH,
    PRODUCT_PATH,
    MENNU_PATH,
    PRIVATE_KEY,
    EXPIRESIN,
    AccessKeyID,
    AccessKeySecret
} = process.env